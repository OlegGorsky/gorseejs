# Content Site Example

Canonical example for a mature product-style public content or marketing site.

This example demonstrates the recommended Gorsee path for:

- prerendered landing pages
- public cache semantics
- content routes with minimal client JavaScript

Imports:

- `gorsee/client` for page UI and head management
- `gorsee/server` for explicit public cache policy
