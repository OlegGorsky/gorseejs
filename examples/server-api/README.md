# Server API Example

Canonical example for a mature product-style API-first server app.

This example demonstrates the recommended Gorsee path for:

- API-first route tree with `gorsee/server` primitives
- explicit runtime topology and security baseline
- process/service deployment instead of browser bundles
