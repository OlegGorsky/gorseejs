# Workspace Monorepo Example

This example is part of the mature product surface for Gorsee.

It shows the recommended workspace / monorepo path with one app package and one shared package, without hiding runtime ownership inside shared code.
